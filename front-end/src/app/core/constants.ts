import { MatTooltipDefaultOptions } from '@angular/material/tooltip';

export const TOOLTIP_DEFAULT_OPTIONS: MatTooltipDefaultOptions = {
    showDelay: 350,
    hideDelay: 0,
    touchendHideDelay: 0,
    disableTooltipInteractivity: true,
};
